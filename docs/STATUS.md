# STATUS — E-commerce Stonni (Dashboard)

> Atualizado: 2026-09-23

## Documentação

| Arquivo | Conteúdo | Quando abrir |
|---|---|---|
| `docs/META_CAPI_E_RELATORIOS.md` (01/09) | guia do Meta: atribuição, relatórios de campanha e CAPI — inclui o handoff do repoint do Tintim, a regra de leitura da conversão e o estado do CAPI | antes de mexer em ROAS, CAC, atribuição ou em qualquer número que venha do Meta Ads |
| `docs/sql/2026-09-17_preflight_vw_ecom_docs_datas.sql` + `2026-09-17_ensaio_aceite_vw_ecom_docs_datas.sql` | pré-voo e teste de aceite da view `vw_ecom_docs_datas` (data de criação do pedido a nível de documento) | antes de mexer em `use-faturamento.ts` ou em qualquer card que compare "faturado" com "pedido feito" |
| `docs/2026-09-17-design-system.md` | receituário do design system Stonni interno aplicado (tokens, ponte, o que ficou de fora) | antes de mexer em cor/fonte/medida em qualquer tela, ou antes de criar tela nova |
| `docs/2026-09-22-diagnostico-atendimento-vendedor.md` | diagnóstico da planilha manual de atendimento x app: fecha quem são os 9 nomes (Guilherme=atacado, Blas=ex-funcionário), descarta vínculo Umbler pra Henrique Trombini/Michael (já decidido antes) e Matheus/Klevertton (ex-funcionários), e abre 2 itens reais: `convertido` nunca preenchido em `ecom_leads`, `ecom_sdr_atendimentos` parado desde 09/09 | antes de propor vincular qualquer vendedor novo em `ecom_umbler_vendedor`, ou antes de mexer na métrica de conversão do Atendimento |

## O que é
Dashboard do e-commerce Stonni: faturamento por canal, marketing (Meta Ads), marketplace (ML/Shopee), atendimento (funil + tempo de resposta) e relatórios. App de **baixo uso** — o Leo autorizou "mandar bala" (quebrar não é problema, ≠ atacado).

## Onde está
- **Clone real (git):** `C:\Aplicações da bononi\bononiecommerce` (remote `leobononi2906/bononiecommerce`, branch `main`). Confirmado em 17/09/2026 — `temp_clone` em `C:\CLAUDE\...` mencionado aqui antes **não existe mais**; esta é a única pasta. **SEMPRE `git fetch` antes** (tem sessão paralela commitando na main).
- **Deploy:** https://bononiecommerce.vercel.app · push na `main` → Vercel automático.
- **Supabase:** `vishxwdxqiygbxmtpfoy` (tabelas/views `ecom_*`, intake `umbler_*`).
- **Stack:** React/Vite com **inline styles + CSS vars próprias** (`--blue-dark`, `--green`…), **NÃO Tailwind** (o `@import "tailwindcss"` foi removido do `index.css` em 17/09 — não fazia nada, nenhuma classe utilitária era usada). Hooks em `src/hooks/`.
- **Design system:** Stonni interno, aplicado em 17/09/2026 — ver `docs/2026-09-17-design-system.md`. Tokens em `src/ds/stonni-ds.css` (não editar à mão), ponte em `src/index.css`.

## Fluxo de dados (Umbler)
Migrado pro **intake único** (passo 3): canais OFICIAL LV/LF → edge `umbler-intake` → `Ecomm_UMBLER` v83 (downstream fino, só cria `ecom_leads`/`ecom_leads_fila_bot`). Raw vive em `umbler_eventos.payload`. Funil de conversão = `ecom_atd_funil` (cron `ecom-atd-refresh`, jobid 45) cruzando contato Umbler × faturamento ERP por `bononi_telefone_key`.

## Estado atual (produção, 26/08/2026)
- **Home**: cards com comparativo "vs período anterior" + aviso de mês parcial. Faixa "Líquido após devolução externa" (bruto→devolução→líquido). Seção "Tráfego — retorno sobre investimento": ROAS geral = (faturamento site + vendedores) ÷ investimento Meta Ads no período (fix 26/08, base pedida pelo Leo — tráfego alimenta os dois canais, não só o site), CAC = investimento ÷ pedidos, Ticket médio do site. Desde 18/09, "Faturamento Vendedores" (e a devolução/ROAS/CAC que dependem dele) também só soma quem tem vínculo Umbler↔ERP ativo — mesmo fix da tela Vendedores.
- **Marketing**: Meta Ads com faixa de tendência 6m + aviso de mês parcial. Pipeline de atribuição antigo (Meta Ads × TikTim × `ecom_campanha_subgrupo`, `useCampaignVerdicts`) continua aqui, sem mudança.
- **Campanhas — ROI real** (`/campanhas-roi`, nova 26/08, sub-aba de Marketing): fonte única `vw_ecom_campanha_roi` — sem pipeline manual. KPIs (Investimento/Leads/CPL/Faturamento/ROAS) + tabela ordenável por investimento/leads/compraram/faturamento/ROAS, chip verde/vermelho, aviso "s/ atrib." em campanhas antigas (pré-atribuição, `leads_nossos=0`).
- **Marketplace**: tendência por canal (ML Battogo/Bononi/Full/Shopee), faturamento **LÍQUIDO de devolução** (25/08) e, desde 17/09, **pela data do pedido, não da fatura** (ver dev-log), pedidos, ticket, Δ R$/% vs período anterior, sparkline 6m, barra empilhada, coluna Devolução. (Removida toda a gestão de estoque ML Full.)
- **Vendedores**: ranking **por líquido** (26/08, devolução por `id_vendedor`), KPIs bruto→devolução→líquido; desde 18/09 só entra quem tem vínculo Umbler↔ERP ativo (exclui atacado e quem já saiu); filtro "Personalizado" persiste (18/09, não se perde mais no auto-refresh de 5min).
- **Relatórios**: agrupamento produto/vendedor/mês com filtros amplos (canal/vendedor/grupo/subgrupo/produto), **líquido por linha** (26/08, devolução respeitando os mesmos filtros das vendas), export CSV.
- **Atendimento**: funil de vendedores (`FunilVendedores.tsx`) com nova coluna **Faturamento (R$)** por vendedor (26/08, `valor_venda_erp` de `ecom_atd_funil`); KPI "Vendedores ativos" agora lê `vw_ecom_vendedores_ativos` (fonte = Hub, Config→Umbler→Usuários — o ecom só lê, não cadastra); mapa de calor 24h; TMR via `vw_ecom_tempo_resposta` (mediana, fonte `umbler_mensagens`).
- **Devolução externa** (25–26/08): `vw_ecom_devolucao_externa` amarra devolução→venda ONLINE (`interna=false`), traz `id_vendedor`/`subgrupo`. Aplicada em Home, Marketplace, Vendedores e Relatórios. Detalhe completo na doc do app.

## Correção de base (26/08/2026) — "faturamento do site sumido"
**Não era bug de render** (a Home já tinha o card "Faturamento Site"). A plataforma migrou de `SITE` pra `TRAY` (Tray Commerce) entre abr–jul/26; `SITE` zerou e `TRAY` virou 100% do canal, mas `getCanal()` não reconhecia `TRAY` e jogava tudo pro bucket "vendedor" (inflando o ranking de vendedores e zerando o site). Fix: `SITE_NAMES` em `src/lib/query.ts` agora inclui `'TRAY'`. Corrige de uma vez Home, Vendedores, Marketplace e Relatórios (todos usam o mesmo `getCanal()`).

## Pendências / próximos passos
- [x] ~~Cadeia `ecom_sdr_*` (conversão por atendimento individual, tipo "FECHOU/INTERESSADO/...") tinha o consumidor externo travado desde 2026-09-09 13:25:27 UTC~~ — resolvido em 23/09: a causa não era um serviço externo quebrado, era **não haver nada chamando a função de normalização**. `ecom_sdr_sync_norm()` é uma função Postgres autocontida (lê `umbler_eventos`, grava nas tabelas normalizadas, avança `norm_checkpoint`) — não depende de LLM nem de nada fora do banco, só precisava ser invocada, e nunca achamos por quem/onde (não é cron, não é Edge Function, sem trigger, sem Database Webhook — conferido de novo, mesmo resultado do dia anterior). **Aplicado:** rodei `ecom_sdr_sync_norm()` direto (testada antes em `--ensaio`) pra colocar os 14 dias em dia — 41.123 mensagens, 2.068 transferências, 45 vendas, checkpoint em dia — e criei `cron.job` **`ecom-sdr-sync-norm`** (jobid 66, `*/10 * * * *`) chamando essa mesma função, pra parar de depender do processo externo desconhecido. Ver dev-log 23/09.
- [ ] **`ecom_leads.convertido` está zerado em 28.100 de 28.100 linhas — a coluna existe mas nunca foi preenchida.** `vw_ecom_vendedores.convertidos` usa nota fiscal `ONLINE` por vendedor como proxy, não "este atendimento virou esta venda" de verdade. Decisão de produto, não bug: construir a amarração lead→venda (esforço médio) ou aceitar o proxy atual. Ver `docs/2026-09-22-diagnostico-atendimento-vendedor.md` §3.A.
- [ ] **Os 63 leads que sobraram sem vínculo não são do e-commerce.** `aTGhkpoXrJLt7_rY` (39, Henrique Trombini/ERP 7686) e `aTG6AL5d9I0UBGsZ` (22, Michael/ERP 47544) têm **0 docs ONLINE em 6 meses** — são loja física e O.S. **Não vincular**: vinculá-los colocaria venda de balcão no ranking do e-commerce. Some `aYOfFB11Ou5bM8fo` (2 leads). O caminho certo é separar esses atendimentos na Umbler, não no dashboard.
- [x] ~~🔴 Meta cortou o acesso à API (Data Use Checkup vencido)~~ — resolvido em 22/09: Meta aprovou o checkup em 1 dia (bem antes do prazo de até 10 avisado). `ecom-meta-sync` invocada com `{"dias": 30}` recuperou 23/08–21/09 inteiro (402 registros, R$19.575,26), incluindo os 7 dias que tinham ficado perdidos (10–16/09). Conferido na Home em produção: o aviso multi-dia sumiu, sobrou só o aviso normal de atraso de 1 dia (defasagem esperada do cron diário). Ver dev-log 22/09.
- [x] ~~O card "Faturamento Site" fica enviesado pra baixo no mês corrente~~ — resolvido em 17/09 com o aviso "Pedido de hoje represado" (ver dev-log). Cobre Site e Marketplace.
- [ ] **O já-faturado de Vendedores ainda conta pela data de faturamento, não pela data do pedido** (diferente de Site/Marketplace, corrigidos em 17/09). Ganhou em 21/09 uma correção parcial — passou a somar também pedido já finalizado no SGA sem nota (ver dev-log) — mas decisão do Leo foi não estender a data-do-pedido pro que já tem nota agora: Vendedores não tem indício de lote represado como os outros dois canais, mas não foi conferido a fundo.
- [ ] **71 pedidos do lote de Bling represado (R$ 23.011,15) ainda não têm documento no ERP.** Quando o SGA finalmente faturar essas 71 notas, `data_criacao` delas vai nascer com a data de hoje (do faturamento), não a data real do pedido — porque `vw_ecom_docs_datas` deriva de `vw_comercial_itens_margem`, que só existe depois que o documento é criado. Pra esses (só esses — os outros 711 já têm `data_criacao` certa) vai precisar da tabela de exceção `ecom_doc_data_real` desenhada na investigação de 17/09 (ver dev-log), preenchida por match `data+cliente+valor±R$300` depois que o SGA os criar.
- [ ] **6 pedidos do site foram faturados sob o vendedor `ML MLB PR` e caíram no card Marketplace em vez do Site — R$ 9.536,47.** Achado ao reconciliar (21/09) o extrato baixado da plataforma do site com o Supabase: `getCanal()` (`src/lib/query.ts`) classifica qualquer `nome_vendedor` começando com `'ML '` como marketplace, e o ERP passou a lançar venda de site sob esse vendedor desde 28/08. Pedidos #181/#182/#183/#184/#185/#195 — docs 21699/21938/21936/21931/21930/22362, todos `id_empresa=2`, casados por CPF+data+frete idêntico ao centavo. Conserto é reatribuir `nome_vendedor` no SGA; não temos escrita lá hoje — ver `pendencias-firebird` item 7 (atualizado nesta data: já tinha só o pedido #195/doc 22362, agora com os outros 5).
- [ ] **2 notas fiscais parecem em dobro em `vw_comercial_docs_margem` — R$ 14.575,26, precisa de olho humano.** Mesmo cliente, mesmo valor, 2 NFs diferentes, só 1 pedido correspondente no extrato da plataforma do site (achado na mesma reconciliação de 21/09): Instituto de Pesquisa e Reabilitação de Animais Marinhos (R$ 6.745,00 — NF 8796 de 01/07 e NF 8848 de 15/07, empresa 3) e Francisco Augusto Cardoso (R$ 7.830,26 — NF 9591 de 21/07 e NF 9695 de 28/07, empresa 2). Pode ser reemissão após cancelamento (conta faturamento em dobro) ou segunda compra real (está certo) — não dá para decidir só com o banco. **Perguntado ao Leo em 2026-09-23: ele precisa conferir no Bling/Nuvemshop antes de responder** — segue aberto até ele voltar com o resultado.
- [x] ~~Publicar a Edge Function `ecom-meta-sync` corrigida~~ — publicada pelo painel do Supabase em 21/09 (ver dev-log). Não dá pra confirmar por comportamento que é a versão nova rodando: enquanto a Meta bloqueia a conta inteira, o erro sai idêntico nas duas versões. Confirmação de verdade só quando o acesso voltar.
- [ ] **`vw_ecom_subgrupos` e `vw_ecom_campanha_conversao` continuam raspando o timeout** — hoje seguram porque o retry do front cobre. O conserto de verdade precisa de índice em `vw_comercial_itens_faturados`, que é recarregada inteira ~500x pelo `rep_swap`: medir o impacto no swap antes de criar qualquer coisa lá.
- [ ] **Margem líquida do marketplace (frete+taxas) — BLOQUEADA em achar a tabela certa.** Leo pediu pra descontar frete+comissão do card "Faturamento Marketplace". `vw_comercial_docs_faturados` já tem `taxa_marketplace`/`valor_frete`, mas funciona só pro ML (Shopee tem `taxa_marketplace=0`, gap de dado). O Leo mandou print do ERP com "FRETE E-COMMERCE PGTO"/"COMISSAO E-COMMERCE" (código ~164486+) — confirmado Firebird antigo, mas não é `TBL_MOVIMENTO` (não bate com `vw_fb_movimento_base`, que só vai até cod_movimento 114329). Falta achar a tabela certa e escrever extração nova no `bononi-replicador`. (Havia aqui, até 15/09, um ponteiro para um "ESTADO_ATUAL_APP" em `docs/` que **não existe** — o detalhe que importava é o desta linha.)
- [ ] **Mover OFICIAL LV/LF pra Aplicação "GERAL SUPABASE"** na UI da Umbler e remover o webhook dedicado antigo (só o Leo faz). Enquanto coexistir = entrega dupla (não quebra).
- [ ] Otimizar `vw_ecom_campanha_conversao` (500 intermitente por timeout; com filtro `mes_ref` responde 200) — usada pelo pipeline antigo de Marketing, não pela nova aba ROI. **Investigado de novo em 22/09:** reportado como travado (spinner infinito, 9× 500), mas num reload limpo (sem carga própria em paralelo) a tela `Campanhas` carregou normal em ~10s, números batendo com Home/Campanhas-ROI. Contenção provável (5 queries próprias da tela + testes em paralelo na mesma janela), não quebra de código. Decisão: manter como está — o índice que resolveria de vez segue sem medir o impacto no `rep_swap` (mesma pendência de antes), e não vale mexer em cima de um problema que não reproduziu limpo.
- [x] ~~Se o Leo quiser, aplicar líquido de devolução também no gráfico 6m por vendedor
      (Home/Vendedores) e no pivô de produtos (Marketplace)~~ — **feito em 2026-09-23, o Leo
      confirmou que sim.** Ver dev-log. De quebra, achado e corrigido um "completa o vazio"
      novo no caminho: o pivô de produtos escondia como `·` qualquer mês negativo (produto que
      devolveu mais do que vendeu) — trocado `v > 0` por `v !== 0` nas 3 células que faziam isso.

## Dívidas e armadilhas conhecidas
- **Bundle antigo em cache no navegador** foi a causa real de vários "bugs" que o Leo viu (Home em branco, #id/produtos vazios) — **hard refresh / redeploy** resolve; as queries e CORS estão OK.
- TMR antiga (`vw_ecom_espera_vendedor`, filtro `etapa='ECOMMERCE...'`) **morreu na migração Umbler** — não usar; casar por telefone dá tempos absurdos.
- Tabelas `ecom_umbler_conversas/mensagens` e `ecom_debug_webhook` (1,4 GB) foram dropadas/truncadas — o raw agora é `umbler_eventos.payload`.

## Dev-log
- 2026-09-24 — **Service worker novo: pega versão nova sozinho, sem F5.** App Vite/React sem
  service worker, risco baixo (bundles com hash) mas sem controle explícito de cache do
  `index.html`. `public/sw.js` novo (network-first pra navegação/`index.html`, cache permanente
  pra `/assets/*` hasheado, ignora chamadas a Supabase/outra origem), `src/lib/pwa.ts` registra
  e recarrega sozinho no `controllerchange` (importado em `src/main.tsx`, pulado em dev).
  `vercel.json` ganhou `Cache-Control: no-cache` em `index.html`/`sw.js` (rewrite de SPA e
  headers existentes preservados). Roteamento (`BrowserRouter`) já mantinha a tela pela URL —
  não mexido. Reclamação era geral do grupo (mesma correção replicada em `bononi-hub`,
  `com_stonni`, `bononi-dashboard`, `parceiro-stonni`, `stonni-assistencia` e nos outros apps
  Vite), modelo copiado do `bononi-exped`.
- 2026-09-23 (3) — **Devolução aplicada onde ainda ficava bruto "de propósito", decisão do Leo
  ao revisar as pendências do board.** `Faturamento por vendedor — últimos 6 meses`
  (`Vendedores.tsx`) somava só `faturamento_doc`; agora soma também `useDevolucao6Meses()` como
  negativo na mesma agregação por mês×vendedor — mesmo critério do ranking líquido logo acima,
  que já descontava. `Vendas por produto — últimos 6 meses` (`Marketplace.tsx`) ganhou um hook
  novo, `useMarketplaceDevolucaoProdutos6Meses` (mesma query de `vw_ecom_devolucao_externa`, mas
  com `referencia` — a devolução por vendedor já existente não tinha esse campo), casado por
  referência+mês; a métrica Qtd continua bruta de propósito (devolução é peça que voltou, não
  venda a menos — misturar as duas contaria giro de estoque como se fosse queda de faturamento).
  **Achado corrigindo isso**: a tabela de produtos escondia como `·` qualquer valor negativo
  (`v > 0 ? fmtVal(v) : '·'`) — com líquido, um produto pode devolver mais do que vendeu num mês
  específico, e isso ficaria invisível bem na hora que mais importa. Trocado por `v !== 0` nas 3
  células que faziam essa conta (linha do produto, total da linha, total da coluna), com `R$` em
  vermelho quando negativo. `npx tsc -b` limpo; conferido local com dado de teste (gráfico de
  vendedores e pivô de produtos carregando líquido, toggle Qtd continua bruto).
- 2026-09-23 (2) — **"[SGA] Marketplace x Faturamento divergindo" — pendência do board pessoal
  (Trello "Rotina Bononi" → Hoje) fecha como já corrigida em 17–21/09.** O card não tinha
  descrição, só duas cifras (R$ 1.092.546,92 x R$ 518.654,14, razão ~2,11x). Não achei
  snapshot histórico pra reproduzir os dois valores exatos, mas reproduzi o **mecanismo**:
  simulando o método antigo de contar Marketplace (por `data_faturamento`, antes do fix de
  17/09) contra Faturamento Vendedores no mesmo mês, a razão medida foi **2,26x**
  (R$ 1.183.022,48 x R$ 522.826,71) — mesma ordem de grandeza e mesma direção do card.
  - **Causa:** o SGA fatura pedido de marketplace em lote e atrasado (um pedido de abril pode
    virar nota só em setembro). Contar pela data da nota fazia o mês corrente engolir calado o
    backlog represado, inflando o Marketplace até ~2x contra um canal (Vendedores) sem esse
    padrão de lote — daí o descasamento. **Já corrigido e publicado**: `comDataDePedido()`
    (`src/hooks/use-faturamento.ts`) e `fetchMktComDataDePedido()` (`src/hooks/use-marketplace.ts`)
    passaram a contar pela data do pedido (via `vw_ecom_docs_datas`), não pela emissão da nota —
    ver entradas de 17/09 e 21/09 abaixo.
  - **Ressalva, não corrigida:** a tabela "Vendas por produto — últimos 6 meses" da aba
    Marketplace continua somando **bruto por `data_faturamento`** (de propósito — o rótulo já
    avisa "bruto, sem descontar devolução"), enquanto o KPI do topo da mesma tela já é líquido
    por pedido. Se a comparação que gerou o card foi entre essas duas, ainda existe; se foi entre
    Home e Marketplace (o caso mais provável), já está fechada. `useMarketplaceProdutos6Meses`
    em `src/hooks/use-marketplace.ts` é onde estender `comDataDePedido` se algum dia quiserem
    unificar os dois.
- 2026-09-23 — **Achado e religado o consumidor do `ecom_sdr_*` — pendência do board pessoal
  (Trello "Rotina Bononi" → Hoje, "[Atendimento IA]").** Reconferi contra produção o diagnóstico
  de 22/09 (§3.B): `norm_checkpoint` continuava travado em 2026-09-09T13:25:27, backlog tinha
  crescido de 70.880 pra 77.042 eventos, e a busca por trigger em `umbler_eventos`/`ecom_*`,
  Database Webhook (`supabase_functions.hooks` nem existe neste projeto) e nos 80 Edge Functions
  publicados (`supabase functions list`) não achou nada relacionado — confirma que é mesmo
  externo ao Postgres e ao Supabase Functions.
  - **A virada:** em vez de continuar procurando o consumidor externo, procurei os `pg_proc` que
    referenciam `ecom_sdr` e achei `ecom_sdr_sync_norm()` — função sem parâmetro, `SECURITY
    DEFINER`, que faz o trabalho INTEIRO sozinha (lê `umbler_eventos` desde o checkpoint, faz
    upsert em `ecom_sdr_mensagens_norm`/`_transferencias_norm`/`_fechamentos_norm`, chama
    `ecom_sdr_refresh_rollups`, avança `norm_checkpoint`). Não chama nada de fora — só precisa
    ser chamada. O "consumidor externo" nunca foi uma pipeline complexa: era só o que quer que
    invocasse essa função de tempos em tempos, e isso parou em 09/09 sem deixar rastro.
  - **Testado com `--ensaio` antes de aplicar** (roda de verdade, desfaz tudo): 40.711 mensagens,
    2.060 transferências, 45 vendas, 3.560 chats, ~20-35s, zero erro. Só depois disso apliquei de
    verdade.
  - **Aplicado em produção:** `select ecom_sdr_sync_norm();` direto — **41.123 mensagens, 2.068
    transferências, 45 vendas upsertadas**, checkpoint em `2026-09-23T13:29:49`. Conferido lendo
    `ecom_sdr_config` depois: bate. (O `sql.py` imprimiu o resultado sob o rótulo `ERRO` — é só
    como o script formata um `map[...]` do Go, não é falha; confirmado pelo objeto, não pela
    palavra — mesma armadilha já registrada em `supabase-cli-so-ultimo-resultado`.)
  - **Criado `cron.job` `ecom-sdr-sync-norm`** (jobid **66**, `*/10 * * * *`, comando `select
    public.ecom_sdr_sync_norm();`) — assim o Atendimento IA para de depender de um processo
    externo que ninguém sabe onde mora. Verificar daqui a uns dias que `norm_checkpoint`
    continua andando (cron "succeeded" só prova que rodou, não que a função fez o que devia —
    mesmo cuidado do caso do Bling).
  - **Não mexi** em `analisar_ecom_sdr`/`analisar_ecom_sdr_gateway` (a etapa de classificação
    FECHOU/INTERESSADO/etc.) — são funções à parte, chamadas sob demanda com período+vendedor,
    não fazem parte do que estava travado.
- 2026-09-22 (1) — **Meta aprovou o checkup em 1 dia (não os até-10 avisados) — backfill recuperou o buraco inteiro, e a Home confirma.** Fecha o incidente aberto em 16/09.
  - Invocação direta com `{"dias": 30}` (chave anon) voltou `ok:true`: janela 23/08–21/09, 402 registros, **R$19.575,26** recuperados. `dias_gravados` inclui os 7 dias que a versão antiga teria perdido pra sempre (10 a 16/09) — e como só a versão nova devolve `desde/ate/dias_gravados`, essa resposta também fecha a dúvida de ontem sobre qual versão estava publicada (era a nova).
  - **Conferido na Home em produção** (`bononiecommerce.vercel.app`, sem login): o aviso "Investimento em tráfego incompleto" mudou de multi-dia pra **"faltam 1 dia de carga"** — a defasagem normal até o cron de amanhã rodar, não mais incidente. ROAS/CAC continuam só com a cautela padrão de 1 dia, não o alerta grave que tinha desde 16/09.
- 2026-09-21 (8) — **`buscarTudo` (`src/lib/query.ts`) não aceita mais página curta/vazia como fim sem confirmar.** Corrige o truncamento sob carga concorrente achado na entrada (7) de hoje (mesma consulta variou 566/1129/7066 linhas).
  - **Causa não fechada por completo:** não dá pra confirmar daqui se o truncamento é do PostgREST, do Supavisor (pool sob contenção) ou de outra camada — não tenho acesso aos logs de infra do Supabase. A correção foi desenhada pra ser correta independente de qual seja: em vez de tentar identificar o mecanismo, ela para de confiar cegamente numa página curta.
  - **Fix:** antes de aceitar uma página mais curta que `PAGINA` (inclusive vazia) como fim de dados, `buscarTudo` sonda 1 linha exatamente no ponto onde parou (`paginar(de, de)`). Achar linha na sondagem prova que a página anterior tinha sido truncada — em vez de perder o resto calado, a linha sondada entra no total e a busca volta a paginar normalmente a partir dali. Só confirma "acabou" quando a sondagem vem vazia.
  - **Descartei a alternativa óbvia** (pedir `count: 'exact'` do Supabase e comparar contra o total) por dois motivos: exigiria tocar os 45 pontos de chamada de `buscarTudo` em 8 hooks (todos no formato `(de,ate) => supabase.from(...).select(...).range(de,ate)`, sem `count` hoje), e pesaria mais nas views que já raspam o timeout de 8s (`vw_ecom_subgrupos`, `vw_ecom_campanha_conversao`, `vw_comercial_*`). A sondagem de 1 linha fica só em `query.ts` — **zero mudança nos 8 hooks**, todos os 45 pontos de chamada ganham o conserto automaticamente, e reproduz exatamente o padrão observado (truncamento não-determinístico, não um teto fixo).
  - **Testado:** 6 cenários isolados rodando a função REAL (`npx tsx`, import direto de `query.ts`, sem reimplementar a lógica numa cópia) — incluindo reproduzir o bug relatado ponto a ponto (7066 linhas reais, 1ª página cai pra 566 como na produção) e confirmar que agora fecha em 7066, não em 566 nem 1129; página vazia no meio da busca; truncamento em 3 pontos diferentes da mesma busca (contenção sustentada, não só a 1ª chamada); página final curta de verdade não gera sondagem desnecessária (2 páginas + 1 sondagem, não mais que isso); filtro amplo demais (>50 requisições) continua estourando erro claro, não trava nem devolve parcial calado. `npx tsc --noEmit` e `npx oxlint src/lib/query.ts` limpos. Local (porta 5180, banco de TESTE): Home carregou com os mesmos números de antes (Marketplace R$301.690,86, Site R$108.148,54, tabela 6 meses, leads, subgrupos) sem erro novo no console — o único erro em tela ("Vendedor finalizado sem nota") é o 404 de `vw_vendas_sem_faturamento` não existir no banco de teste (já documentado na entrada de 21/09 (6)), não relacionado a esta mudança.
  - **Custo:** +1 requisição pequena (LIMIT 1) por chamada de `buscarTudo` na maioria dos casos (a sondagem do fim), e mais algumas só quando há truncamento de verdade — aceitável frente ao risco de subcontar calado num dashboard financeiro.
  - Feito com outra sessão editando o mesmo repo em paralelo (ela commitou e limpou a árvore antes do meu commit — `git status` conferido antes de editar e de novo antes de commitar, sem `git add -A`).
- 2026-09-21 (7) — **A tabela "Por departamento — últimos 6 meses" da Home passa a usar o mesmo critério dos cards de "período selecionado".** Achado investigando "os valores não estão batendo" (Leo comparou os dois blocos da Home): a tabela contava Site/Marketplace pela emissão da nota fiscal, os cards já contavam pela data do pedido desde 17/09 — como o SGA fatura marketplace em lote, com meses de atraso, a linha do mês corrente na tabela chegava a ~2x o valor do card equivalente.
  - `useFaturamentoSite6Meses` (`use-faturamento.ts`) e `useMarketplace6MesesPorPedido` (`use-marketplace.ts`) — os mesmos `comDataDePedido`/`fetchMktComDataDePedido` que os cards já usavam, agora também para a janela de 6 meses (`getPeriodRange('6_meses')`). Vendedores na tabela ganhou o filtro `erpAtivos` que só o card tinha (`Home.tsx`).
  - Achado junto, bug de verdade: o card "Faturamento Marketplace" descontava a devolução externa duas vezes — `useMarketplaceCanais().fatAtual` já vem líquido, e `Home.tsx` descontava de novo em `liq.marketplace`. O valor rotulado "bruto" saía líquido. `MktCanal` ganhou `fatBrutoAtual`/`fatBrutoAnt` (bruto de verdade); `Home.tsx` passou a usar `totalBrutoAtual`/`totalBrutoAnt`.
  - Confirmado por SQL direto em produção, não só pela leitura do código: R$21.084,86 (site) e R$535.883,69 (marketplace bruto), por data do pedido, set/26 — bate com o que a tabela mostra agora publicada.
  - Pendência nova: a tabela ainda não inclui "pedido finalizado no SGA sem nota" no canal Vendedores (feature que outra sessão está adicionando ao card em paralelo, mesmo dia) — por isso a coluna Vendedores da tabela (R$481.540,71 em set/26) pode ficar abaixo do card (R$493.071,71) enquanto essa feature não for estendida pra cá; não é bug.
  - Testado: `npx tsc --noEmit` limpo nos 4 arquivos tocados. Confirmado ao vivo em produção depois do deploy (commit `69a9edb`) — não só localmente: sob carga concorrente (outra sessão editando o mesmo repo ao mesmo tempo), o helper de paginação `buscarTudo` truncou resultado sem erro (mesma consulta variou entre 566/1129/7066 linhas em tentativas sucessivas) — achado à parte, não é bug desta correção, sinalizado como tarefa futura.
  - Feito com outra sessão editando o mesmo repo ao mesmo tempo — Leo autorizou seguir; commit isolado só de `use-faturamento.ts`/`use-marketplace.ts`/`useData.ts`/`Home.tsx`, sem tocar nos arquivos que ela ainda tinha em aberto.
- 2026-09-21 (6) — **Faturamento Vendedores passou a somar também pedido já finalizado no SGA (status='F') que ainda não tem nota fiscal.** Pedido do Leo depois de perguntar se o card já filtrava por "finalizado" (não filtrava — ver entrada (1) do mesmo dia, que só explicava a diferença).
  - Achado ao investigar: `vw_comercial_docs_margem` (fonte do card) só traz documento já faturado, sem coluna de status nenhuma. O único lugar com o status do SGA (`TBL_VENDA.STATUS`, F=finalizado/A=aberto) pro canal vendedor é `vw_vendas_sem_faturamento` — só as vendas que AINDA não geraram nota. Universo ONLINE conferido em produção: 9 pedidos em todo o histórico, 4 reais (2 "(sem vendedor)" sem vínculo Umbler↔ERP, 1 é ML BONONI/marketplace) — R$~13,2 mil no total, nada de lote represado antigo tipo Site/Marketplace.
  - Escopo combinado com o Leo (2 perguntas antes de mexer): mostrar a lista completa antes de decidir corte de idade por "pedido parado há muito tempo" — decidiu não precisar, porque os dois casos que pareciam arriscados na lista (um pedido de 315 dias parado, um de R$25,9 mil ainda "aberto") já ficam de fora sozinhos: o primeiro não tem vínculo Umbler↔ERP ativo, o segundo não é status='F'. E decisão de não mexer no critério do que **já tem nota** — continua pela data de faturamento, não pela data do pedido (diferente do fix que Site/Marketplace ganharam em 17/09); fica registrado como parte da pendência ainda aberta.
  - `fetchFaturamentoVendedorSemNota` (`use-faturamento.ts`) — nova query em `vw_vendas_sem_faturamento`, filtrada `tipo_saida=ONLINE`, `tipo_doc=VENDA`, `status=F`, pela data do PEDIDO (`data_venda` — único campo de data que a linha tem antes de faturar). Formato compatível com `FAT_COLS`; `id` sai negativo pra nunca colidir com o id real (sequence positiva) de `vw_comercial_docs_margem`. Sem custo/margem — a view não traz, então só soma no bruto.
  - `Home.tsx` e `Vendedores.tsx`: `fatPVend`/`fatAntVend` concatenam o já-faturado com o novo pendente-finalizado antes de entrar em `somaCanais`/`ranked` — resolve o nome de exibição pelo vínculo Umbler↔ERP (`nomeAtivoPorId`) quando a linha vem com "(sem vendedor)" mesmo tendo `id_vendedor` com vínculo ativo (achado nesta investigação: id 85193 = MAYKELL, vínculo ativo, mas `vw_vendas_sem_faturamento.vendedor` vem em branco pra ele). Os dois banners que explicavam "conta só pela nota" (escritos mais cedo hoje, entrada (1)) foram reescritos — o texto antigo ficou errado com a mudança.
  - Testado: `npx tsc --noEmit` e `npx oxlint` limpos nos 4 arquivos tocados. Local (porta 5180, banco de TESTE) não tem `vw_vendas_sem_faturamento` — confirmado erro `PGRST205` (view não existe em staging, não é bug do código); os dois cards degradam certo (mostram "–", não zero, com aviso "Não carregou" e botão de retry). Query real conferida direto em produção pela REST anônima (mesma chave pública do bundle já publicado): `200 OK`, devolve exatamente os 3 pedidos de setembro esperados (ML BONONI, MAYKELL sem nome, Alex Sandro).
  - Feito com outra sessão editando o mesmo repo ao mesmo tempo (`use-faturamento.ts`, `Home.tsx`, `useData.ts`, `STATUS.md` também mudaram por ela) — Leo autorizou seguir; edições por trecho, sem `git add -A`.
- 2026-09-21 (5) — **Checkup de dados da Meta enviado — mas enviar não libera a API, só a aprovação libera.** Fecha parcialmente o item aberto em 16/09 (`ecom-meta-sync` parada por revisão anual de uso de dados vencida).
  - Acesso ao App Dashboard da Meta (app "Bononi Acessórios", id 494212378509321) foi conseguido, e as 4 etapas da "Renovação do acesso a dados" foram completadas: Conexão com empresa (já OK), Uso permitido (só a permissão `public_profile` aparece pra revisão — a leitura de anúncio do `ecom-meta-sync` roda por usuário de sistema do Business Manager, não entra nessa lista), Tratamento de dados (respostas pré-preenchidas conferidas contra o que o código faz: sem operador de dados terceiro, responsável declarado Kauan Cardozo, sem solicitação de autoridade nos últimos 12 meses) e Instruções do analista (política de privacidade em `accessoriosbononi.com.br/privacidade`, conferida carregando de verdade — não só validando o formato da URL). Status após o envio: "Em análise", Meta avisa até 10 dias.
  - **Testado por invocação direta logo depois do envio — o erro continua idêntico ao de 16/09.** Enviar o checkup não reabre a API sozinho; o rombo de investimento não gravado (~R$750/dia desde 16/09) continua enquanto durar a análise.
  - **A Edge Function corrigida (pendente desde 16/09) foi publicada pelo painel do Supabase**, trocando a versão que buscava só "ontem" pela versão com janela de backfill (`{"dias": N}`, padrão 7, até 90 dias). Publicar agora foi deliberado: enquanto o acesso segue cortado a função é um no-op de qualquer versão — sem risco — e evita depender de alguém lembrar de publicar no dia em que a Meta liberar.
  - **Não deu pra confirmar por comportamento qual versão está rodando.** A invocação pós-deploy só prova que não travou no boot (voltou JSON válido) — o erro da Meta sai idêntico nas duas versões enquanto a conta inteira está bloqueada. A confirmação real só vem quando o acesso voltar: a versão nova, em caso de sucesso, retorna `desde/ate/dias_gravados`, campos que a antiga não tem.
  - **Ficou de fora, registrado em Pendências:** invocar com `{"dias": 30}` pra recuperar 10–16/09 assim que a Meta aprovar — a janela padrão de 7 dias da versão nova não alcança mais essas datas a partir de agora.
- 2026-09-21 (4) — **Conferência Bling: a separação por direção virou um sub-filtro "Maior no: Ambos/Bling/ERP" dentro de Preço e Estoque, em vez de dois botões extras só pra estoque.** Ajuste pedido pelo Leo em cima da entrada (3) — ele queria a mesma separação por direção também no filtro de Preço, e "dentro" do filtro, não como botão solto ao lado. Trocado: `estoque_bling_maior`/`estoque_erp_maior` saíram do enum `Filtro`; entrou um estado `Direcao` (`todas`/`bling_maior`/`erp_maior`) próprio, com um segundo grupo de botões que só aparece quando o filtro ativo é "Preço" ou "Estoque". `difPreco`/`difEstoque` (mesma base `envioDe`: positivo = ERP/manual maior, negativo = Bling maior) decidem o lado. `src/pages/ConferenciaBling.tsx`. Testado: `npm run build` (tsc + vite build) sem erro; **sem teste clicado no navegador** — porta 5180 seguia ocupada por outra sessão.
- 2026-09-21 (3) — **Conferência Bling ganhou dois filtros pra separar o lado da divergência de estoque: "Bling > ERP" e "ERP > Bling".** Pedido do Leo depois de perguntar o que a tela `/conferencia` fazia — só existia o filtro "Estoque" (qualquer diferença, sem dizer quem está maior). Os dois botões novos comparam o valor que seria efetivamente enviado (`estErp`, ou o manual quando "Sincronizar" está desmarcado) contra `estBling`, com margem de 0,001 pra não confundir arredondamento com divergência real — mesma base que o filtro "Estoque" já usava (`envioDe`/`mudaEstoque`), só separada por direção. `src/pages/ConferenciaBling.tsx`. Testado: `npm run build` (tsc + vite build) sem erro; **sem teste clicado no navegador** — a porta 5180 (dev server) estava em uso por outra sessão no momento da publicação.
- 2026-09-21 (2) — **Os dois avisos âmbar da Home ("Mês em andamento" e "Pedido represado no faturamento") pintavam o parágrafo inteiro de âmbar, não só o ícone/título — baixo contraste, ilegível.** O Leo mandou print reclamando. Causa: os dois blocos eram `<div>` inline duplicados com `color:'var(--amber)'` no container inteiro, em vez de reusar o `AlertBanner` já existente (que não força cor no texto). `AlertBanner` (`src/components/ui/index.tsx`) ganhou a prop opcional `icon` — quando presente, troca o pontinho por um ícone `lucide-react` colorido, corpo do texto continua herdando `--text-primary` (padrão do design system Stonni: só ícone+título levam a cor do tom, corpo fica normal — mesma regra do `Alert.jsx` do design system, que não estava sendo seguida aqui). Os dois banners de `Home.tsx` agora usam `<AlertBanner type="warning" icon={...}>`, sem duplicar estilo. O banner de Vendedores (dot, sem `icon`) não foi tocado — comportamento antigo preservado por padrão quando `icon` não é passado. Testado local: ambos os banners renderizam com ícone âmbar e corpo em texto normal, sem erro de console; Vendedores continua igual.
- 2026-09-21 (1) — **Home e Vendedores ganharam aviso de que "Faturamento Vendedores" conta pela emissão da nota fiscal, não pela venda.** Pedido original citava os 3 canais ("marketplace e site também mudam quando é faturado"), mas isso só é verdade hoje pra Vendedores: `fetchFaturamento` (`use-faturamento.ts`) filtra direto por `data_faturamento`, sem a correção `comDataDePedido` que Site e Marketplace já ganharam em 17/09 (ver dev-log daquele dia). Por isso o aviso ficou restrito a Vendedores — `AlertBanner` no topo de `Vendedores.tsx` (mesmo padrão do banner "leads sem vínculo" logo abaixo) explicando a diferença com o SGA, que conta pela venda finalizada; e uma ressalva curta na legenda "Faturamento por canal" da Home, citando só esse canal. Testado local (`npm run dev -- --port 5180`): os dois avisos renderizam sem erro de console, dado carregou normalmente.
- 2026-09-18 (4) — **Home: os 3 cards de "Faturamento por canal" (Vendedores/Site/Marketplace) ganharam uma linha "líquido" abaixo do valor bruto.** Pedido do Leo. `KpiCard` (`src/components/ui/index.tsx`) ganhou a prop opcional `liquido`, renderizada entre o valor principal e o "vs anterior" — não troca o valor bruto do card, só acrescenta a linha. Usa o `liq.vendedor/site/marketplace` que já existia (calculado pra seção "Total ONLINE líquido", só não aparecia por canal). Testado local: os 3 cards mostram "líquido: R$ X" e o total bate com a soma dos 3.
- 2026-09-18 (3) — **Home: o mesmo fix do ranking de Vendedores, estendido ao card "Faturamento Vendedores" e ao que depende dele.** O Leo perguntou se o card da Home era bruto — era, e ainda somava atacado/ex-funcionário porque `Home.tsx` tem sua própria agregação por canal (`somaCanais`/`somaDevolucao`), sem o filtro que entrou na tela Vendedores. Agora as duas funções só contam canal "vendedor" quando `id_vendedor` tem vínculo Umbler↔ERP ativo (mesmo `erpAtivos` de lá) — afeta "Faturamento Vendedores", devolução por canal, "Total ONLINE" (bruto e líquido), e ROAS/CAC (que usam receita e pedidos de site+vendedor). Precisou trazer `id_vendedor` pra `DEV_COLS` em `use-devolucao.ts` (só tinha `nome_vendedor`, sem como filtrar por id). O gráfico "últimos 6 meses" (bruto, inclui quem já saiu) não foi tocado — mesma razão da tela Vendedores.
- 2026-09-18 (2) — **Vendedores: ranking e KPIs vinham somando vendedor de ATACADO e gente que já saiu.** O Leo notou Guilherme S., Blas C., Daniel S. e Matheus F. no ranking — Guilherme é do atacado, os outros três já saíram da empresa. Causa: `getCanal()` classifica como "vendedor" qualquer nome do ERP que não seja site/marketplace, sem checar se é vendedor de e-commerce ativo de verdade. Confirmado em produção: nenhum dos 4 tem vínculo em `ecom_umbler_vendedor` (os 5 vendedores reais têm, com `ativo=true`). Fix: `ranked` em `Vendedores.tsx` agora exige vínculo Umbler↔ERP ativo e não-interno (`erpAtivos`), a mesma régua já usada em outra tela para "vendedores ativos". O gráfico "últimos 6 meses" continua incluindo quem já saiu — é intencional (legenda própria) e o vínculo deles foi apagado da tabela, então não dá pra separar "saiu" de "nunca foi vendedor" só por ela; ficou registrado como possível resíduo do Guilherme lá.
- 2026-09-18 (1) — **Vendedores: filtro "Personalizado" se perdia sozinho a cada 5 minutos.** O auto-refresh da tela chamava `window.location.reload()`, que remontava o app e voltava pro período "Mês atual" sem aviso — e nenhuma tela persistia o período num F5 manual, só o padrão fixo configurável em Configurações. Fix: o reload de 5min agora refaz os fetches em vez de recarregar a página, e o período (fixo ou personalizado) é persistido numa chave própria (`stonni_periodo_atual`, separada de `stonni_periodo_default` que é o padrão configurável). Testado local: período "01/09–10/09" sobrevive a reload de página.
- 2026-09-17 (11) — **Tentativa de trazer a nota fiscal de marketplace pro "Histórico de vendas" via `exp_documentos`, com efeito quase nulo — registrado pra ninguém tentar de novo sem saber disso.**
  - O Leo notou que o app **bononi-exped** (Expedição) tem a nota de marketplace, e pediu pra investigar como. Achado: a Expedição não usa o ERP pra isso — ela puxa **direto da API do Bling** a cada 15 min (`exp-sync-bling`, 3 crons por conta: `bononi`/`mlb`/`battogo`) e grava em `exp_documentos`, tabela própria dela, no mesmo projeto Supabase. 100% de cobertura ali, mas **sem `id_doc`/`id_empresa`** — não tem chave direta pro documento do ERP que o e-commerce lê.
  - A única chave em comum possível é `chave_nfe`. Uma amostra inicial (empresa MLB PR, jun-jul, misturando `tipo_doc='O.S.'`) sugeriu 24% de cobertura — mas **medido de novo, restrito a `tipo_saida='ONLINE'` (venda de verdade), a cobertura real é ~0%**: 0 de 2.021 docs de marketplace em jun-jul, 12 de 1.365 em setembro (e desses 12, quase todos já tinham `num_nf` de outra forma). **Lição: número de cobertura de uma amostra que mistura tipo de documento não vale pra decidir nada — sempre restringir ao mesmo filtro que a tela usa antes de confiar num percentual.**
  - Implementei o cruzamento mesmo assim (`numNfPorChave` em `use-relatorios.ts`, usado só quando `num_nf` do ERP está vazio e `chave_nfe` existe) — é seguro (nunca casa errado, só resolve quando a chave bate nos dois lados) e fica pronto pra quando/se o ERP passar a preencher `chave_nfe` de marketplace. **Mas hoje, na prática, quase não muda o que aparece na tela** — o "—" continua a maioria. Decisão do Leo: manter assim mesmo sabendo do efeito mínimo, em vez de reverter ou ler `exp_documentos` sem cruzar com o ERP (que preencheria "solto", sem garantia de ser a linha certa).
  - Nota via cruzamento aparece marcada com `*` na tela (tooltip explica a origem), pra não confundir com nota nativa do ERP.
- 2026-09-17 (10) — **Relatórios ganhou aba "Histórico de vendas"** — lista de venda por venda (data, vendedor, canal, nota fiscal, valor), não agrupada, com busca por vendedor/NF. Pedido do Leo depois de conferir se as 12 vendas de hoje batiam com a planilha manual do atendimento — antes só dava pra ver isso por SQL.
  - `useDocsFaturados` (`use-relatorios.ts`) — nova consulta a nível de **documento** (`vw_comercial_docs_margem`), diferente de `useRelatorioItens` que é a nível de item/produto e já existia. Só busca quando essa aba está selecionada (`enabled`), para não pesar as outras 3 abas que não precisam disso.
  - Reaproveita os filtros de canal e vendedor que a tela já tinha; grupo/subgrupo/produto somem nessa aba (não fazem sentido no nível de documento inteiro). Mostra as 500 mais recentes na tela (com aviso se houver mais) e exporta o CSV completo sem esse limite.
  - Testado local: filtro por número de NF isola a venda certa, contagem e soma batem com o total da tela.
- 2026-09-17 (9) — **A correção do Marketplace estava lenta a ponto de derrubar outras consultas da tela.** O Leo reportou "tá demorando demais" — print mostrou até "Faturamento Vendedores" com erro de carga (um card que nem foi tocado pela correção de hoje).
  - **Causa:** `comDataDePedido` buscava `vw_ecom_docs_datas` filtrando por `id_doc IN (lote de 400)`, em lotes paralelos — para o Site (~470 docs históricos) isso era 1 requisição; para o Marketplace (~8.700 docs históricos) virava **~22 requisições simultâneas**, disputando conexão com o banco e derrubando por timeout até consultas que não tinham nada a ver.
  - **Fix:** filtrar `vw_ecom_docs_datas` por **data** (`data_criacao between start and end`) em vez de por lista de id. O universo "documento criado neste período" é muito menor que "todo o histórico do canal" — medido direto em produção: 2 requisições sequenciais, ~1,9s no total, contra os vários segundos e timeouts de antes. `vw_ecom_docs_datas` não expõe `id` para ordenar a paginação; a tripla `tipo_doc,id_doc,id_empresa` (que já é o `DISTINCT ON` da view) faz esse papel, sendo única.
  - **Conferido de novo em produção pela mesma réplica em Python** que provou a versão anterior: 640 docs / R$437.144,51 pra Marketplace no mês atual — subiu de R$432.076,05 pra R$437.144,51 desde a última checagem, confirmando ao vivo que os pedidos represados vão entrando sozinhos, como o aviso novo promete.
- 2026-09-17 (8) — **Novo aviso na Home: "Pedido de hoje represado no faturamento".** Pedido do Leo, depois de confirmarmos com dado real que o dia corrente (e às vezes o anterior) aparece com bem menos pedido do que o normal em Site e Marketplace — não por queda de venda, mas porque o ERP não fatura o pedido no mesmo dia (dado em 17/09: marketplace foi de ~40 pedidos/dia pra **4** no dia corrente).
  - `diasComPedidoRepresado` (`src/lib/query.ts`) — heurística pura, sem banco: recebe a lista de `data_criacao` do canal no período e a data-fim do período; só avalia se o período alcança hoje, calcula a média dos últimos 7 dias "maduros" (excluindo hoje/ontem) e aponta hoje/ontem como represado se ficarem abaixo de 50% dessa média. Sem histórico suficiente (menos de 5 dias maduros), fica calado em vez de arriscar um falso positivo.
  - `useMarketplaceCanais` passou a devolver também `datasCriacao` (as datas de criação já buscadas para somar o faturamento, sem fazer uma segunda consulta só pra isso). `Home.tsx` cruza isso com `siteP` (que já tinha `data_criacao` por linha) e mostra o nome do(s) canal(is) afetado(s) no aviso.
  - **Não é um número exato de pedidos faltando** (isso exigiria uma fonte de "pedido não faturado ainda", que não existe em nenhuma view) — é um sinal de que o dia está incompleto, para não ler o corrente como queda real. Testado local: aparece corretamente quando o padrão bate, e a lógica foi conferida à mão contra os números reais de produção (17/09: hoje=4 pedidos vs média de 40 nos 7 dias anteriores → 10%, aciona; ontem=35 vs média de 40 → 87%, não aciona).
- 2026-09-17 (7) — **Marketplace ganhou o mesmo conserto do Site: conta pela data do PEDIDO, não pela data de faturamento.** Gatilho: o Leo corrigiu no Bling o status de 782 pedidos represados (ficaram "Aberto" meses, o SGA não puxava) e pediu pra ajustar o Marketplace antes que o lote inflasse mais o mês.
  - **Confirmado com o lote real, batendo pedido a pedido:** dos 782 pedidos do Bling (07/04 a 28/08/2026), 711 (90,9%) foram achados no ERP cruzando `data_criacao` + valor + nome (não existe campo com o número do pedido do Bling em nenhuma view comercial — o identificador exato mora em `exp_sga_pedido_map`, mas essa tabela não carrega `id_doc`). `data_criacao` bateu **exata** com a data do Bling nos 583 casos de match triplo, e nos 118 com diferença de valor a diferença foi **sempre** o frete somado no total do Bling (mediana R$196, nunca negativa) — não é pedido errado.
  - **O tamanho do problema, medido em produção:** 726 dos 1.360 documentos de marketplace faturados entre 01–17/09/2026 (R$556.799,00 de R$988.739,28 — **56,3%**) são pedido de meses anteriores. Pico de **229 notas faturadas de uma vez em 11/09**. Abril e maio estavam **subestimados** em ~R$143 mil e ~R$248 mil pela mesma razão inversa (pedido feito lá, faturado só depois).
  - **Sem migration nova para o grosso do problema** — `vw_ecom_docs_datas` (criada mais cedo hoje, pro Site) já cobre 100% dos 8.648 documentos históricos de marketplace, `data_criacao` neles já está certa. Código: `aggMktPorPedido` (`use-marketplace.ts`) busca os docs do canal (via `id_vendedor` de `useMktCanais`, sem filtro de data — histórico completo) e cruza com `vw_ecom_docs_datas` pela mesma função `comDataDePedido`, extraída de `fetchFaturamentoSite` pra virar utilitário compartilhado (`use-faturamento.ts`) — a busca por data de criação também virou `fetchDocsDatasPorIdDoc`, em lotes de 400 ids (o site tinha poucas dezenas de docs e cabia num `.in()` só; marketplace tem milhares, estourava o tamanho da URL).
  - **`useMarketplaceCanais` mudou de assinatura** (`periodo, ids`) — precisa dos ids do canal (de `useMktCanais`) ANTES de rodar, pra filtrar `id_vendedor` em vez de baixar todo o histórico ONLINE (site+vendedor+marketplace) só pra separar em memória. Único consumidor era `Marketplace.tsx`, reordenado pra chamar `useMktCanais` primeiro. `Home.tsx` ganhou a mesma dependência para o card "Faturamento Marketplace" e os totais "Bruto/Líquido".
  - **Conferido igual em código e em SQL direto** (produção, somente leitura): 635 docs / R$432.076,05 pela nova regra contra 1.360 / R$988.739,28 pela antiga, mesmo número dos dois lados.
  - **Ficou de fora, registrado em Pendências:** os 71 pedidos do lote (R$23 mil) que ainda não têm documento no ERP — quando o SGA os criar, vão nascer com `data_criacao` de hoje (não da data real), e aí sim vai precisar da tabela de exceção `ecom_doc_data_real` (desenhada, não aplicada). E Vendedores continua com a data de faturamento — sem indício de lote represado lá, mas não conferido a fundo.
- 2026-09-17 (6) — **Seletor de período ganhou opção "Personalizado"** (data início/fim), além dos 4 fixos (Mês atual/anterior, 3/6 meses). Pedido do Leo, útil pra conferir janelas específicas (ex.: o período exato de um lote de correção de nota).
  - `Periodo` (`src/types/index.ts`) virou união: os 4 fixos (`PeriodoFixo`, tipo novo) + `{ tipo:'personalizado', inicio, fim }`. `getPeriodRange`/`getPreviousPeriodRange` (`src/lib/query.ts`) tratam o objeto: "anterior" de um recorte personalizado é a mesma duração, terminando no dia anterior ao início (01–17/09 = 17 dias → anterior = 15–31/08).
  - Os 3 `Record<Periodo, string>` que existiam soltos (`AppShell.tsx`, `Marketplace.tsx`, `CampanhasRoi.tsx` — cada um com sua cópia do mesmo texto) viraram duas funções únicas, `periodoLabel`/`periodoLabelAnterior` em `query.ts`. Não dava pra manter os `Record` como estavam: `Record` exige chave string/number, e o novo `Periodo` inclui um objeto — o TypeScript acusou os 3 pontos na hora de compilar, então a centralização não foi opcional.
  - UI: popover com dois `<input type="date">` no botão "Personalizado" do topo; aplica só com início ≤ fim. Não fica salvo como padrão (`stonni_periodo_default` continua só os 4 fixos) — é seleção da sessão, de propósito.
  - Testado local: aplicar 01–17/09 mudou a comparação "vs anterior" da Home (de -54% pra -10% no card de Vendedores, por exemplo — janela de 17 dias contra 17 dias, não mês parcial contra mês cheio) e o rótulo do Marketplace mostrou "01/09–17/09 vs 15/08–31/08" corretamente.
- 2026-09-17 (5) — **Logo real da marca na sidebar e no favicon**, no lugar do ícone Snowflake genérico dentro de um quadrado. Pedido do Leo, depois de aplicar o design system. Símbolo copiado do pacote da skill (`assets/logo-mark.png`, fundo transparente) para `src/assets/stonni-logo-mark.png`; favicon em `public/favicon-stonni.png`. É um dos 3 lugares em que o gradiente da marca pode aparecer (símbolo, avatar, login) — não é decoração fora de regra.
- 2026-09-17 (4) — **Design system Stonni interno aplicado no app inteiro.** Pedido do Leo. Detalhe completo em `docs/2026-09-17-design-system.md`; resumo aqui:
  - Cor primária: azul genérico `#0077CC` → azul da marca `#1A74C4`; cartão em destaque: navy `#1A3A8F` → índigo `#16103D` (mesmo tom da navegação, quando a Home ganhar uma).
  - Tipografia: DM Sans/DM Mono → Archivo (título/KPI) + IBM Plex Sans (corpo/UI) + IBM Plex Mono (identificador/número).
  - **Emoji removido** (regra do pacote, sem exceção): 📅 nos 3 banners de "mês em andamento" virou ícone `Calendar` (lucide-react); 🥇🥈🥉 do ranking de Vendedores virou selo numerado com cor — sem depender só da cor, o número continua visível.
  - **Zero hex fora de `ds/`** (eram ~150) e **zero medida fora da escala de 4px** nos CSS soltos — confirmado pelo `auditar-tokens.py`, as 5 checagens de falha silenciosa passam.
  - **Achado pela própria auditoria antes de publicar:** `--text-hint`, usado em dezenas de lugares no app, não existe com esse nome no DS (lá é `--text-subtle`) — na primeira versão da ponte ficou sem tradução, o que teria deixado todo texto "apagado" da tela com uma cor inválida, sem erro nenhum no console. Corrigido antes do teste local.
  - **Fora desta passada, registrado no doc:** `fontSize`/`lineHeight` dentro de `style=` inline (200+ pontos) continuam número literal — é o mesmo tipo de trabalho que a medida de espaçamento foi, só que maior; convertê-los fica pendente. `tailwindcss` saiu do `index.css` (não fazia nada) mas a dependência continua no `package.json`.
  - Testado local (banco de teste): Home, Vendedores, Marketplace e Configurações renderizam sem erro, valores batendo com antes da mudança — é troca de cor/fonte, não de dado.
- 2026-09-17 (3) — **"Devolução externa" e "Total ONLINE líquido" podiam mostrar "− R$ 0,00" em vez de carregando.** Pedido do Leo: conferir se os 3 cards de "Líquido após devolução externa" batem. Bateram na aritmética (R$1.467.245,93 − R$29.851,51 = R$1.437.394,42, produção, mês atual) — mas na leitura do código os dois cards usavam o `loading` do **faturamento** (`lfp`), nunca o da própria devolução (`useDevolucaoPeriodo`, que nem era desestruturado). Se a devolução demorar mais que o faturamento (paginam separado), a tela mostrava o valor final zerado como se fosse definitivo, mesma família do "0min" de 16/09. `Home.tsx`: `ldp` desestruturado e usado nos dois cards e no `sub` da taxa. Sem mudança de número, só de estado de carregamento.
  - **Achado na mesma investigação, registrado mas NÃO corrigido — ver Pendências:** "Faturamento Site" (fix de hoje mais cedo) só enxerga pedido **já faturado**, então o mês corrente nasce estruturalmente menor e cresce conforme o ERP fatura (setembro por data do pedido: 3 docs/R$16.263,92; agosto, já maduro, pela mesma regra: 26 docs/R$24.032,86). A comparação "vs anterior" do card de Site (e do bruto/líquido que o somam) está enviesada pra baixo o mês inteiro por causa disso — não é erro de conta, é a régua sendo curta no mês corrente.
- 2026-09-17 (2) — **Nenhum valor da aplicação abrevia mais (k/M).** Pedido do Leo: tudo por extenso.
  - `fmtBRL` (`src/lib/fmt.ts`) tinha os dois cortes (`≥1_000_000 → "R$ 1.5M"`, `≥1_000 → "R$ 41k"`) — removidos; agora sempre `toLocaleString('pt-BR', {style:'currency',...})`, igual à antiga `fmtBRLFull` (que virou um alias dela, mantida só por compatibilidade de import). Como todo card, tooltip e eixo de gráfico de todas as páginas usa essa mesma função, uma mudança só resolveu em todo lugar — conferido que não existe nenhuma outra abreviação escrita à mão (`grep` por `toFixed(1)}M` e `/1000` no `src/` não achou nada fora de `%`/`x` de ROAS, que não é dinheiro).
  - **Efeito colateral esperado, não é bug:** a tabela "Por departamento — últimos 6 meses" da Home já era rolável na horizontal (`overflowX:auto`) porque as colunas não cabem juntas; com número por extenso ela rola mais um pouco. Não mexi no layout — se o Leo achar apertado, é ajuste de coluna, não de formatação.
- 2026-09-17 — **"Faturamento do Site" contava faturamento de outros meses como se fosse do período — o card media a data errada.** Pedido do Leo: o valor real na Nuvemshop (sem pedido manual), 01–17/09, era R$ 16.427,59; a tela mostrava R$ 40.788,59.
  - **A causa, provada por SQL:** o card soma pela **data de faturamento** (quando o ERP emite a nota), e não pela **data do pedido**. Em 03/09/2026 o ERP faturou de uma vez um lote de **26 pedidos represados de fevereiro a agosto** (provável encerramento da Tray) — 29 docs no total somavam R$ 40.788,59, mas só **3 foram pedidos de fato em setembro**, somando R$ 16.263,92 (bate com o número do Leo a R$ 163,67, provavelmente 1 pedido ainda não integrado ou diferença de frete). Descartado, com número: duplicação de linha, canal errado, taxa de marketplace em dobro, frete contado duas vezes — nenhum bate, nenhum explica a diferença.
  - **Banco (produção `vishxwdxqiygbxmtpfoy` e teste `gxzhuewczlixksqrmjuk`, aplicado):** view nova `vw_ecom_docs_datas` (`supabase/migrations/20260917120000_ecom_docs_datas.sql`) — 1 linha por documento com `data_criacao`/`data_doc`, que antes só existia a nível de item em `vw_comercial_itens_margem`. Não altera nem apaga nada existente. Pré-voo confirmou **zero** documentos com mais de uma data de criação entre seus itens (a `DISTINCT ON` não escolhe entre valores divergentes) e volume 1:1 com `vw_comercial_docs_faturados` (31.143 = 31.143).
  - **Código:** `use-faturamento.ts` ganhou `useFaturamentoSitePeriodo`/`useFaturamentoSitePeriodoAnterior` — busca os docs do canal site/tray (sem filtro de data, o canal é pequeno) e cruza em memória com `vw_ecom_docs_datas` pela tripla `tipo_doc/id_doc/id_empresa` (as duas views não têm FK para o PostgREST embutir automaticamente), filtrando por `data_criacao` no período. `Home.tsx`: os cards "Faturamento Site" (nas duas seções em que aparece), "Ticket médio (site)", ROAS geral e CAC agora usam esse valor. "Total ONLINE (bruto/líquido)" também soma o site corrigido.
  - **Escopo, decisão do Leo:** só o canal Site foi corrigido agora. Marketplace e Vendedores continuam pela data de faturamento — ver Pendências, porque o Marketplace (ML BONONI FULL) tem o mesmo padrão de faturamento em lote.
  - **Conferido:** ensaio em produção reproduziu exatamente R$ 16.263,92 em 3 docs (join view+dado real, em transação com rollback); pós-conferência depois de aplicar bateu o mesmo número lendo como o app lê (chave anon). Testado também local contra o banco de teste (sem erro nos 4 lugares que usam o dado novo) — números diferentes lá porque o teste tem dado sintético, não é o mesmo caso de setembro.
- 2026-09-16 (fim do dia) — **O Meta Ads não estava "atrasado": o Meta cortou o acesso à API, e o sync perdia dia para sempre.**
  - **Causa, em texto claro da própria API** (invocando `ecom-meta-sync` à mão): `API access disrupted. Go to the App Dashboard and complete Data Use Checkup.` Revisão anual de uso de dados vencida — resolve-se no App Dashboard do Meta, não aqui.
  - **Não é anúncio pausado, e isso foi verificado antes de concluir qualquer coisa.** Cruzando `umbler_lead_origem` (que só ganha linha quando chega lead COM `ad_id`) com `ecom_meta_ads` dia a dia: de 10 a 16/09 entraram 127 a 186 leads de anúncio **por dia**, no mesmo patamar dos dias com carga — com zero de custo registrado. Anúncio no ar, custo faltando.
  - **O token NÃO estava morto**, hipótese que parecia óbvia e era falsa: `meta_ad_cache` (alimentada por `meta-resolver-ad`, mesma credencial) atualizou em 14/09 e `umbler_lead_origem` hoje. A suspensão atingiu o endpoint de `insights`.
  - **Por que ninguém viu por uma semana:** o cron 16 chama `net.http_post`, que é **assíncrono** — ele marca `succeeded` em 0,1s só por ter enfileirado a requisição, mesmo quando a função devolve 500. E `net._http_response` tem retenção de horas, então o erro some antes de alguém procurar.
  - **O que foi corrigido no código:** a função buscava **só o dia de ontem**, então dia que falhava não era buscado nunca mais — é a explicação dos buracos soltos em 08/05, 14-15/07, 18/07, 30-31/07 e 04/08, que pareciam aleatórios. Agora tem **janela de backfill** (7 dias por padrão, `{"dias": N}` até 90 para recuperação), numa chamada só (`time_increment=1` já devolve por dia) e com o mesmo upsert idempotente. O fonte passou a viver em `supabase/functions/ecom-meta-sync/index.ts` — antes existia **só dentro do banco**.
  - **Não foi possível testar o backfill:** enquanto o acesso estiver cortado, qualquer versão recebe o mesmo erro. O que foi validado é a sintaxe e o caminho de erro. **A função ainda precisa ser publicada pelo painel** (Edge Function não sobe daqui) — ver Pendências.
  - **Também medido e descartado:** índice de expressão em `ecom_tintim_leads` e `umbler_lead_origem` para destravar a `vw_ecom_campanha_conversao`. O ensaio (transação com rollback, em produção) deu 2.602 → 2.018 ms, mas com os **buffers subindo** de 217k para 238k — o ganho é cache, não estrutura. Não foram criados. O gargalo real está no lado dos pedidos, que sai de `vw_comercial_docs_faturados` (recarregada a cada 10 min pelo `rep_swap`), e essa não se indexa sem antes medir o swap.
- 2026-09-16 (tarde) — **O teto de 10.000 linhas não era risco futuro: já estava comendo metade dos leads.** Todo hook usava `.range(0, 9999)`, que **não é paginação, é um teto** — passando de 10.000 o PostgREST corta, responde 200 e não avisa.
  - **Medido antes de mexer:** `ecom_leads` em "últimos 6 meses" tem **21.727 linhas e o app recebia 10.000** — perdia 11.727. Em "3 meses", perdia 1.661. Isso errava o KPI "Leads (período)" da Home, o "Leads Umbler"/"Conversão geral" de Vendedores e o gráfico de leads do Atendimento, sempre **para menos**, sem nenhum sinal na tela. (`ecom_atd_funil`, com 27.006, já escapava porque o funil paginava à mão desde antes.)
  - **`buscarTudo()` em `lib/query.ts`**, agora usado por **todos** os hooks — não existe mais `.range(0, 9999)` no repo. Duas decisões que valem mais que o código:
    - **Página de 5.000.** Paginar re-executa a consulta inteira a cada página (o PostgREST vira LIMIT/OFFSET), então página pequena é cara: nos 21.727 leads, 1.000 → 22 req/14.450 ms, 2.000 → 11 req/8.966 ms, **5.000 → 5 req/5.462 ms**.
    - **Avança pelo que a resposta trouxe, não por `página × tamanho`.** Assim, se o servidor tiver teto por requisição menor que a página (o projeto de teste já esteve com `max_rows=1000`), a busca continua correta em vez de parar achando que acabou. Testado: com um interceptador cortando toda página em 1.000, fez 22 requisições e trouxe os 21.733 mesmo assim.
  - **Toda consulta paginada ganhou `.order()` por chave estável** — `id` onde existe. Sem ordem garantida o Postgres pode devolver a mesma linha em duas páginas e pular outra, que é um jeito **pior** de errar do que truncar. Onde não há `id` a chave é composta: `data,campanha,conjunto` (`ecom_meta_ads`, `vw_ecom_campanhas`), `dt_entrada,telefone` (`ecom_atd_funil`), `mes_ref,campanha` (conversão).
  - **Conferido contra produção**, com o dev server apontado para lá em leitura: "6 meses" passou a mostrar **21.732 leads, o mesmo número do `count(*)` do banco** — nem linha perdida nem duplicada. Home, Vendedores, Marketplace, Relatórios e Atendimento renderizam sem erro; nenhum loop de requisição (estabiliza e para).
  - De quebra, o aviso de falha estreou em produção de verdade: em janela de 6 meses a `vw_ecom_campanha_conversao` falha mesmo com os 3 retries, e a tela **disse isso** em vez de mostrar "nenhuma campanha".
- 2026-09-16 — **Falha de carga parou de virar número na tela, e o "Tempo de resposta" voltou a ser verdade.** Investigação pedida pelo Leo ("os dados não parecem confiáveis").
  - **O sintoma que dava o tom:** a Home/Atendimento mostrava **"Tempo de resposta (mediana): 0min"** — que se lê como atendimento instantâneo — quando o valor real era **11h28min**. Recarregando a mesma tela o número alternava entre os dois, sem nada indicando qual era qual.
  - **Causa:** `vw_ecom_tempo_resposta`, `vw_ecom_campanha_conversao` e `vw_ecom_subgrupos` estouram o `statement_timeout` de 8s **na primeira chamada com cache frio** (1 falha a cada 4–5 tentativas, sempre a primeira — e quem abre um dashboard de gestão é justamente quem pega a chamada fria). O `useQuery` já devolvia `error`, mas **nenhuma página lia** (só Marketplace e Campanhas-ROI): com `data` nulo toda agregação dá 0, e 0 num KPI se lê como fato.
  - **Front:** `useQuery` ganhou **retry** (2 tentativas extras, 400 ms/1200 ms) só para erro transitório (`57014`, `57P01`, `08006`, falha de rede) — erro de schema não repete, para não atrasar a mensagem. Ganhou também `reload`. Novo `AvisoFalhaDeCarga` + `kpiValor` em `components/ui`: o que não carregou aparece como **`–`, nunca `0`**, com banner nomeando a fonte e botão "Tentar de novo". Aplicado em Home, Atendimento, Vendedores, Relatórios e Campanhas. Verificado com `fetch` instrumentado: falhando só a 1ª chamada o valor certo aparece e nenhum banner surge; falhando as 3, o KPI vira `–` e o banner aparece.
  - **Banco (produção, aplicado):** `CREATE INDEX CONCURRENTLY idx_umbler_msg_seg_conversa ON umbler_mensagens (segmento, id_conversa) INCLUDE (direcao, id_membro_umbler, enviado_em, recebido_em)`. O plano trocou `Index Scan + Filter` (lia 176.780 linhas e 127.038 buffers para devolver 2.516) por **Index Only Scan**: **7.454 ms → ~730 ms**, 8/8 chamadas em HTTP 200. Escolhido porque `umbler_mensagens` é append-only de webhook (`n_tup_ins/n_live_tup ≈ 1,0`); **as tabelas `vw_comercial_*` levam ~500 recargas totais do `rep_swap`, e índice novo lá encareceria cada ciclo** — por isso `vw_ecom_subgrupos` e `vw_ecom_campanha_conversao` ficaram só com o retry.
  - **Meta Ads defasado deixou de passar por resultado.** A carga parou em 09/09 (faltam 7 dias; julho também tem buraco de 5). A Home dividia receita do período inteiro por investimento parcial e exibia **ROAS 67,8x e CAC R$ 60,12** sem ressalva. Agora um aviso conta quantos dias faltam e até quando o investimento vai, e os dois KPIs ganham "· parcial".
  - **Conversão geral estava inflada ~25%.** O numerador somava os pedidos de todos os vendedores e o denominador só os leads de quem tem vínculo Umbler↔ERP cadastrado — o Pedro sozinho tirava 662 leads da conta (85 ÷ 2.844 = 3,0% em vez de 85 ÷ 3.567 = 2,4%). O denominador passou a ser todos os leads não-internos, e um aviso mostra quantos estão sem vínculo.
  - **Marketplace parou de esconder canal novo.** `MKT_CANAIS` era uma lista fixa de 4 ids; quando o ERP passou a faturar por **ML MLB PR (id 88661, R$ 108k em set/26)** o canal entrava no total (que usa `getCanal`) e sumia da tabela por produto e do seletor — a mesma tela não fechava consigo mesma. Virou `useMktCanais()`, que descobre os canais do próprio faturamento.
  - **Vínculo do Pedro cadastrado pelo Leo** (`ansbwaMsb2z1sIV6` → `PEDRO HENRIQUE KREIN`, ERP 88118), fechando a correção da conversão: ele saiu de "Conversão –" para **1,5% (10p ÷ 663l)** e os leads sem dono caíram de **726 para 63**. Os 63 que ficaram são de propósito (ver Pendências).
  - **Conferido e OK, não era problema:** as duas views de faturamento batem linha a linha no mesmo recorte (1.430 docs / R$ 1.424.539 em set/26), não há duplicata de `id` e não há venda interna do grupo em `tipo_saida=ONLINE`. O `.range(0, 9999)` ainda não trunca (6.857 linhas em 6 meses), mas está a 69% do teto — ver Pendências.
- 2026-09-11 — **Parceiros: um parceiro pode ter vários acordos, e o drawer parou de cortar campo** (`80e68fc`, `bbb9deb`). Pedido do Kauan: o mesmo parceiro tem modalidades com comissão diferente (10% no Varejo, 5% no Atacado), e o acordo morava **em colunas da linha de `mkt_parceiros`** — então só cabia um valor por parceiro.
  - Tela: o bloco "Acordo" virou lista, com "+ Adicionar acordo" e lixeira por item; cada acordo tem modalidade (autocomplete que cria na hora, igual ao Nicho), tipo, valor, vigência e detalhe próprios. No card, um chip por acordo, e o aviso de vencimento olha a vigência mais próxima entre todos.
  - **Migration 14, aditiva e já aplicada em produção:** tabelas novas `mkt_modalidades` e `mkt_parceiro_acordos`, com backfill do acordo único que já existia. As colunas antigas de `mkt_parceiros` **ficaram** como rede de segurança — quem for limpá-las precisa confirmar que nada mais lê de lá.
  - **Bug de produção corrigido de quebra: editar parceiro existente estava quebrado.** O payload de update mandava o embed (`mkt_nichos`) junto e o PostgREST recusava com **400 em silêncio**, sem tratamento de erro — o salvamento simplesmente não acontecia. Agora só as colunas reais vão no insert/update, e erro de salvamento aparece na tela em vez de sumir.
  - **O drawer cortando campo não era cosmético:** `gridTemplateColumns: 1fr 1fr` sem `minmax(0,...)` não encolhe abaixo do tamanho mínimo do conteúdo, e o `input type=date` do navegador tem mínimo largo. O drawer (fixo em 520px) passava a exigir mais largura que isso e, como a barra de rolagem do app é fina de propósito, Tipo de Acordo e Fim ficavam cortados **sem indicar que dava para rolar**. As 5 grades ganharam `minmax(0,1fr)`, e o drawer ganhou responsividade de verdade (100vw e uma coluna abaixo de 560px). Reproduzido a partir do print do Kauan em ~1440px e testado também em 375px.
- 2026-09-04 — **Campanhas ROI passou a respeitar o seletor de período** (`1b2600d`). A tela lia uma view fixa de 60 dias e **ignorava o período escolhido no topo** — o número mostrado não era o do filtro, que é o tipo de erro que ninguém percebe olhando a tela. Agora chama a RPC `ecom_campanha_roi(inicio, fim)` com o range selecionado, e subtítulo e estado vazio refletem o período.
- 2026-09-01 — **Custo e margem passaram a vir do preço de compra** (`e25fc32`). Atendimento e faturamento leem `custo_doc` (apelidado `custo_doc_pc`) de `vw_comercial_docs_margem`. As views `vw_ecom_produtos`, `_subgrupos`, `_marketplace` e `_vendedores_comercial` foram **repontadas no banco** para base de custo = preço de compra — a mudança é no banco, não só aqui, então comparar margem com relatório antigo dá diferença por definição.
- 2026-08-31 — **O gráfico por vendedor da Home mostrava quem já saiu e escondia quem entrou** (`b0a0e84`). O ranking era por total de **6 meses**, então ex-vendedores (Lucas, Cristian, Matheus, Felipe) seguravam vaga e **o Pedro, que é novo, caía em "Outros"** — o gráfico retratava o semestre passado, não o mês. Agora os nomes são de quem vendeu no **mês atual**, e quem parou vai para Outros. O tooltip virou customizado: esconde entrada de R$ 0,00 e ordena do maior para o menor.
- 2026-08-26 (tarde) — **ROAS/CAC corrigido** (base pedida pelo Leo: tráfego alimenta site+vendedores, não só campanha atribuída) — em produção. **Margem marketplace investigada e BLOQUEADA** (ver Pendências acima) — não mexi no card "Faturamento Marketplace", continua bruto.
- 2026-08-26 — **Comandos do cérebro (4 itens)**: (1) nova aba `/campanhas-roi` (fonte `vw_ecom_campanha_roi`); (2) fix `SITE_NAMES` inclui `TRAY` — resolve "site sumido" de vez, Home ganhou ROAS geral/CAC/Ticket médio; (3) "Vendedores ativos" lê `vw_ecom_vendedores_ativos` (Hub); (4) coluna Faturamento (R$) no funil via `valor_venda_erp`. Também: devolução externa estendida pra Vendedores (ranking por líquido) e Relatórios (líquido por linha, respeitando filtros); view `vw_ecom_devolucao_externa` ganhou `subgrupo`.
- 2026-08-25 — **Marketplace líquido de devolução por canal** (PR #1, mergeado). `use-marketplace.ts`: `aggMktDev` lê `vw_ecom_devolucao_externa` e abate por canal (mesma classificação `getCanal`/`normMkt` do faturamento); série 6m neta com devolução como fat negativo. `Marketplace.tsx`: coluna Devolução + KPI "Total Marketplace (líq.)". Tabela de produtos fica bruta (mix/volume). Só externa abate; atribuição = mês da devolução. Validado ao vivo (jul: ML Battogo 170k, Full 91k, Bononi 141k, Shopee 78k). Home/Vendedores já netavam.
- 2026-08-07 — TMR revivida (`vw_ecom_tempo_resposta*`, mediana); lote Atendimento/Relatórios/Vendedores.
- 2026-08-06 — Remodela: Marketplace por canal, comparativos + aviso de mês parcial, TMR removida do ranking.
- 2026-07-31 — Migração pro intake único (`Ecomm_UMBLER` v83); funil repontado p/ `bononi_telefone_key`; conversão +15 vendas/+R$86k.
